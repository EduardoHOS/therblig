#!/usr/bin/env node
// The whole package. `npx -y therblig-mcp` is the string that appears in every client
// config, plugin manifest and install badge, so it gets its own name on npm rather
// than hiding behind `npx -y -p therblig therblig-mcp`.
import 'therblig/mcp/bin';
